base: formal_120_sixview_gpu.yaml

experiment:
  output_dir: rebuttal/results/ablations

planners:
  enabled:
    - ray_gpis_novelty_only
    - ray_gpis_uncertainty_only
    - ray_gpis_pointwise
    - ray_gpis_hit_only
    - ray_gpis
